export default function HomePage() {
  return (
    <main style={{ padding: 24, fontFamily: "Inter, Arial, sans-serif" }}>
      <h1>Trash Talk API Scaffold</h1>
      <p>
        API routes are live at <code>/api/commentary</code> and <code>/api/voice</code>.
      </p>
      <p>Frontend MVP can now plug into these endpoints.</p>
    </main>
  );
}
